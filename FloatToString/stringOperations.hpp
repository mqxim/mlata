#pragma once
#include <string>
#include "stringOperations.cpp"

template<typename type> char digitToString(type number);

std::string reverseString(std::string str);

std::string intToString(std::string str);
